{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean",
            "type": "shell",
            "command": "stack clean"
        },
        {
            "label": "build",
            "type": "shell",
            "command": "stack build",
            "problemMatcher": [
                "$go"
            ]
        }
    ]
}